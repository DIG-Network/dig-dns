# dig-dns
*.dig local name resolution for the DIG Network — DNS responder + HTTP gateway (origin+proxy forms) that resolves &lt;storeId>.dig to the latest anchored root + resource keys via the dig-node. OS-service; installed by dig-installer.
